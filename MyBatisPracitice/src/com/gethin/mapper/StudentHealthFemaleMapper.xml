<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.gethin.mapper.StudentHealthFemaleMapper">
<select id="findStudentHealthFemaleByStudentId" parameterType="int" resultType="StudentHealthFemale">
    SELECT  * from t_student_health_female where student_id=#{id}
</select>
</mapper>
